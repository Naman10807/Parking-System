# Leave unset in local dev to use Vite proxy (/api -> http://localhost:8081).
# VITE_API_BASE_URL=/api

# Direct backend URL (requires CORS on Spring Boot):
# VITE_API_BASE_URL=http://localhost:8081/api
